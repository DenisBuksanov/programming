# Отчет по практической работе №16: Знакомство с POSIX потоками

**Выполнил:** Буксанов Д.К., группа ИКС-531

## Задания 1 и 2: Создание и ожидание потока
**Описание выполнения:**
Была написана функция `zapusk_12()`, в которой создается дочерний поток с помощью функции `pthread_create()`. Дочерний поток (функция `potok()`) в цикле 5 раз выводит на экран сообщение и засыпает на короткое время с помощью `usleep()`.
Чтобы родительский поток не завершился раньше времени и дождался выполнения дочернего, используется функция `pthread_join()`. После полного завершения работы дочернего потока, родительский также в цикле 5 раз выводит свое текстовое сообщение.
![Исходный код заданий 1 и 2](2026-05-16%2011.55.08.png)

## Задание 3: Передача параметров потока
**Описание выполнения:**
В функции `zapusk_3()` создается 4 независимых потока. Имеется массив строк (указателей на `char`), содержащий элементы: "Первый", "Второй", "Третий", "Четвертый".
В функцию создания потока последним аргументом передается элемент этого массива. Каждый из четырех потоков выполняет функцию `potok_prmtr()`, которая принимает переданный аргумент, приводит его обратно к типу `char*` и выводит на экран вместе со своим уникальным идентификатором (ID потока). Затем родительский поток в цикле дожидается завершения всех четырех созданных потоков.
![Исходный код задания 3](2026-05-16%2011.55.24.png)

## Задания 4 и 5: Отмена и очистка ресурсов
**Описание выполнения:**
В функции `zapusk_45()` реализован механизм принудительного завершения потока извне. Дочерний поток выполняет функцию `otmena()`, в которой запускается бесконечный цикл `while(1)`, каждую секунду выводящий строку "[Дочерний] Работаю...".
Главный (родительский) поток ждет 2 секунды с помощью `sleep(2)`, а затем вызывает `pthread_cancel(tid)` для отмены дочернего потока.
Чтобы при отмене поток корректно освободил занятые ресурсы, внутри него используется макрос `pthread_cleanup_push()`, который регистрирует специальную функцию очистки `och_otm()`. При получении сигнала на отмену поток прерывается, вызывает эту функцию очистки, и на экран выводится сообщение: `[Очистка] Поток отменен. Ресурсы очищены.`.
![Исходный код заданий 4 и 5](2026-05-16%2011.55.34.png)

## Задание 6: Алгоритм Sleepsort
**Описание выполнения:**
Реализован нестандартный алгоритм сортировки массива чисел с использованием потоков, обладающий заявленной асимптотикой O(N).
Дан исходный массив чисел: `4, 2, 19, 5, 18`. В функции `zapusk_6()` для каждого числа массива создается отдельный поток. В качестве аргумента в поток передается само значение числа (через динамическое выделение памяти `malloc`, чтобы разные потоки не перезаписали общую память).
В функции потока `sleepsort()` каждый поток засыпает на количество секунд, равное переданному числу (`sleep(val)`). Поскольку потоки, получившие меньшие числа, просыпаются раньше, они первыми выводят свои числа на экран. Таким образом, числа естественным образом выводятся в консоль в отсортированном по возрастанию порядке по мере пробуждения потоков. Перед завершением работы каждый поток очищает выделенную память с помощью `free()`.
![Исходный код задания 6](2026-05-16%2011.55.49.png)

## Общий запуск (Main)
Все написанные функции последовательно вызываются в главной функции `main()`.
![Главная функция main](2026-05-16%2011.55.57.png)

## Вывод
Реализованы механизмы управления потоками. На практике применены функции создания потоков, ожидания их завершения, передачи данных через аргументы и корректного прерывания работы с последующей очисткой ресурсов. Также был реализован и протестирован многопоточный алгоритм Sleepsort.